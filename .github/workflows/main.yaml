name: Build

on: 
  push:
    branches:
      - '**'

jobs:
  build:
    name: Build Image
    runs-on: ubuntu-latest
    container:
      image: espressif/idf:v4.4
    steps:
    - uses: actions/checkout@v2
    - run: |
        . $IDF_PATH/export.sh
        idf.py build
        echo $(ls)
    - name: Upload ERC Error Report
      uses: actions/upload-artifact@v3
      with:
        name: build_artifacts
        path: reflow_oven_controller/reflow_oven_controller/build
  
